{% comment %}
  Cart page — Add these too (association / complementary; excludes cart items).
  Add in Theme Editor → Cart → Add block → RecoAI: Add these too
{% endcomment %}
{% render 'reco-widget-root',
  block: block,
  placement_type: 'cart',
  placement_key: 'cart',
  strategy: 'association_rules'
%}

{% schema %}
{
  "name": "t:cart_reco.name",
  "target": "section",
  "templates": ["cart"],
  "stylesheet": "reco-widget.css",
  "settings": [
    {
      "type": "paragraph",
      "content": "t:cart_reco.info"
    },
    {
      "type": "text",
      "id": "api_url",
      "label": "t:shared.settings.api_url.label",
      "info": "t:shared.settings.api_url.info"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "t:shared.settings.heading.label",
      "default": "Add these too"
    },
    {
      "type": "range",
      "id": "item_count",
      "min": 1,
      "max": 12,
      "step": 1,
      "label": "t:shared.settings.item_count.label",
      "default": 4
    },
    {
      "type": "select",
      "id": "layout",
      "label": "t:shared.settings.layout.label",
      "options": [
        { "value": "grid", "label": "t:shared.settings.layout.options.grid" },
        { "value": "carousel", "label": "t:shared.settings.layout.options.carousel" }
      ],
      "default": "carousel"
    },
    {
      "type": "select",
      "id": "columns",
      "label": "t:shared.settings.columns.label",
      "info": "t:shared.settings.columns.info",
      "options": [
        { "value": "2", "label": "2" },
        { "value": "3", "label": "3" },
        { "value": "4", "label": "4" }
      ],
      "default": "4"
    },
    {
      "type": "checkbox",
      "id": "show_price",
      "label": "t:shared.settings.show_price.label",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_atc",
      "label": "t:shared.settings.show_atc.label",
      "default": true
    }
  ]
}
{% endschema %}
