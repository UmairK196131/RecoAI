{
  "name": "recoai",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.19 <22 || >=22.12"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "extensions/*"
  ],
  "scripts": {
    "build": "npm run db:generate && npm run build:extension && npm run build --workspace=apps/web",
    "build:extension": "node tools/reco-theme/build.mjs",
    "dev": "npm run dev --workspace=apps/web",
    "dev:lite": "npm run dev:lite --workspace=apps/web",
    "lint": "npm run lint --workspace=apps/web",
    "typecheck": "npm run typecheck --workspace=apps/web",
    "db:generate": "dotenv -e .env -- npm run db:generate --workspace=packages/database",
    "db:migrate": "dotenv -e .env -- npm run db:migrate --workspace=packages/database",
    "db:migrate:dev": "dotenv -e .env -- npm run db:migrate:dev --workspace=packages/database",
    "db:seed": "dotenv -e .env -- npm run db:seed --workspace=packages/database",
    "test:sprint-5": "dotenv -e .env -- npx tsx apps/web/scripts/test-sprint-5.ts",
    "shopify": "shopify"
  },
  "devDependencies": {
    "@shopify/cli": "4.6.0",
    "dotenv-cli": "^8.0.0",
    "esbuild": "^0.25.12"
  },
  "overrides": {
    "@graphql-tools/url-loader": "8.0.16",
    "@graphql-codegen/client-preset": "4.7.0",
    "@graphql-codegen/typescript-operations": "4.5.0",
    "minimatch": "9.0.5",
    "vite": "^6.2.2"
  }
}
