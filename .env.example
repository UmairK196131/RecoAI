# Shopify App
SHOPIFY_API_KEY=
SHOPIFY_API_SECRET=
SCOPES=read_products,read_orders,read_customers,read_inventory,read_content
SHOPIFY_APP_URL=https://your-tunnel-url.example.com

# Database (PostgreSQL 15 + pgvector via docker compose)
DATABASE_URL=postgresql://recoai:recoai@localhost:5432/recoai

# Redis (via docker compose)
REDIS_URL=redis://localhost:6379

# Optional: custom shop domain for development
# SHOP_CUSTOM_DOMAIN=
