name = "web"
roles = ["frontend", "backend"]
webhooks_path = "/webhooks/app/uninstalled"

[commands]
predev = "npm --prefix ../.. run db:generate"
dev = "npm --prefix ../.. run db:migrate && npm exec remix vite:dev"
