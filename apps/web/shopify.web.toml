name = "web"
roles = ["frontend", "backend"]
webhooks_path = "/webhooks/app/uninstalled"

[commands]
predev = "npm --prefix ../.. run db:migrate"
dev = "npx dotenv -e ../../.env -- npm exec remix vite:dev"
