/** HuggingFace model id loaded via @xenova/transformers (384-dim output). */
export const EMBEDDING_MODEL_ID = "Xenova/all-MiniLM-L6-v2";

/** Stored in product_embeddings.model_version for rollback (NFR-MAINT-02). */
export const EMBEDDING_MODEL_VERSION = "all-MiniLM-L6-v2@1.0";

export const EMBEDDING_DIMENSIONS = 384;
